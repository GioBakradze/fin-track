import React from 'react';

const Reports = React.createClass({
    render() {
        return (
            <div>
                there are also reports
            </div>
        )
    }
});

export default Reports;
